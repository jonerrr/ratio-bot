kind: pipeline
type: exec
name: default

platform:
  os: linux
  arch: amd64

steps:
  - name: build
    commands:
      - npm i
      - npm run build
  - name: start
    commands:
      - node dist/src/index
